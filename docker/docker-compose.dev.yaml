name: ephoros-server-dev

services:
  simulator:
    build:
      dockerfile: ../docker/Dockerfile
      context: ../server
      args:
        SERVICE: simulator
    networks:
      - default
    env_file: ../.env
    depends_on:
      broker:
        condition: service_healthy

networks:
  default:
    driver: bridge
